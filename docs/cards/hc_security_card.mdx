---
hide_table_of_contents: true
title: Security Card
---

import CodeBlock from '@theme/CodeBlock';
import MyComponentSource from '!!raw-loader!/dashboard/HaCasa/templates/hc_security_card.yaml';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

The `hc_security_card` is used for enabling or disabling your alarm. It's configured to set the `Arm Away`, `Arm Home` or `Disarmed`. When the alarm is triggered, the card will turn to red and the icon will flash constatly.


![security Card Light](/img/cards/hc_security_card/securitycard_light.png#light-mode-only)![security Card Dark](/img/cards/hc_security_card/securitycard_dark.png#dark-mode-only)

## Usage

<Tabs>
  <TabItem value="usage" label="Usage" default>
    ```yaml
    type: custom:button-card
    template: hc_security_card
    entity: <your_alarm_entity>
    variables:
      hc_security_code: <your_code>
    ```
  </TabItem>
  <TabItem value="template" label="Template">

    <CodeBlock language="yaml">{MyComponentSource}</CodeBlock>

</TabItem>
</Tabs>

## Variables

| Variable | Default | Required | Description|
|----------|---------|----------|------------|
| security_code | / | **Yes** | The code to arm or disarm the alarm |
| icon_color | var(--color-blue) | No | Color of the icon |
